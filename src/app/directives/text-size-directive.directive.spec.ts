import { TextSizeDirectiveDirective } from './text-size-directive.directive';

describe('TextSizeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSizeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
