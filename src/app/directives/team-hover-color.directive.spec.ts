import { TeamHoverColorDirective } from './team-hover-color.directive';

describe('TeamHoverColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamHoverColorDirective();
    expect(directive).toBeTruthy();
  });
});
