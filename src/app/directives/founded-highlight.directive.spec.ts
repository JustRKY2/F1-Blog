import { FoundedHighlightDirective } from './founded-highlight.directive';

describe('FoundedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FoundedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
